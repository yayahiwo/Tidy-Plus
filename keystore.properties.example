# Copy to keystore.properties (this file is ignored by git).
# Use absolute paths or paths relative to the repo root.
storeFile=/absolute/path/to/tidy-release.jks
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME

# Alternative (CI-friendly): set env vars instead of using keystore.properties:
# - TIDY_KEYSTORE_PATH
# - TIDY_KEYSTORE_PASSWORD
# - TIDY_KEY_ALIAS
# - TIDY_KEY_PASSWORD
